<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
	http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.1.xsd">
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/admin/*" access="hasRole('admin')" />
		<access-denied-handler error-page="/error" />
		<form-login login-page="/login"
			authentication-failure-forward-url="/login?error" username-parameter="username"
			password-parameter="password" />
		<logout logout-url="/login?logout" logout-success-url="/welcome"
			delete-cookies="account_id" />
		<!-- csrf protection -->
		<csrf />
	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT account_id FROM Account WHERE username=? AND password=?"
				authorities-by-username-query="SELECT role FROM Account_Role WHERE account_id=(SELECT account_id FROM Account WHERE username=?)" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>